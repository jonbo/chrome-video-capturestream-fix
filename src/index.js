import { polyfill } from "./polyfill";
import {
  captureStream,
  DEFAULT_FRAMERATE,
  createVideoCanvas,
  getAudioStream,
} from "./captureStream";

export { polyfill };
export { captureStream, DEFAULT_FRAMERATE, createVideoCanvas, getAudioStream };
